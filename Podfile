# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'homework' do
  use_frameworks!
  pod 'Auth0', '~> 1.19'
  pod "PromiseKit", "~> 6.8"
  pod "PromiseKit", "~> 6.8"
end

target 'Data' do
  use_frameworks!
  pod 'Auth0', '~> 1.19'
  pod "PromiseKit", "~> 6.8"
end

target 'Domain' do
	use_frameworks!
	pod "PromiseKit", "~> 6.8"
end